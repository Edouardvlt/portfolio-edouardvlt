name: Deploy Stack
on:
  push:
    branches:
      - master  # Se déclenche quand tu pousses sur la branche main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Portainer Webhook
        run: |
          curl -X POST "${{ secrets.PORTAINER_WEBHOOK_URL }}"
